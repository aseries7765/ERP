// Requires @nestjs/testing and/or a test DB — unavailable in the build
// sandbox (no network access). See ./README.md for what IS verified.
describe.skip('condition.step (needs DB/Nest — see test/workflow/README.md)', () => {
  it('placeholder — fill in once pnpm install can reach the real monorepo', () => {});
});
