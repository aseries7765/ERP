# Stage 57 Exceptions

- PostgreSQL/API runtime is unavailable in the current execution environment.
- Stage 52, Stage 53, and Stage 56 runtime evidence cannot be fabricated.
- Release PASS is therefore unavailable until the guarded runtime harnesses execute against a disposable environment and their retained evidence is independently reconciled.
